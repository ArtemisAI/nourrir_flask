# -------------------------------------------------
# Example environment configuration for NourrIR Flask app on Render.com
# Copy this file to `.env` (local dev) or add the same key-values in Render →
# Environment → Secret Files / Environment Variables.
# -------------------------------------------------

# -------------------------------
# App runtime
# -------------------------------
FLASK_ENV=production
PORT=8080

# -------------------------------
# SMTP (contact form)
# -------------------------------
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=465
SMTP_USERNAME=NO_REPLY@example.com
SMTP_PASSWORD=YOUR_APP_PASSWORD

# Optional ("true" to use STARTTLS instead of implicit TLS)
# SMTP_USE_TLS=false

# -------------------------------
# n8n public URL (for iframe & nav link)
# -------------------------------
N8N_PUBLIC_URL=https://your-n8n-service.onrender.com

# -------------------------------
# LLM backends (already have safe defaults)
# -------------------------------
# OLLAMA_URL=http://localhost:11434/api/chat
# OLLAMA_MODEL=gemma3:latest
# GEMINI_API_KEY=your-key

# Log file
# LOG_FILE=nurih_app.log
